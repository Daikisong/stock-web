name: NSLAB disabled superseded ARM dispatcher 20180619 20260714T172911Z

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04-arm
    steps:
      - run: echo superseded-by-raw-only-dispatcher
